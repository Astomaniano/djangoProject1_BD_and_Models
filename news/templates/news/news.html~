{% extends 'my_app/layoute.html' %}
{% load static %}

{% block title %}
    <title>Новостная страница</title>
{% endblock %}



{% block content %}

    <h1>Новостная страница</h1>

    <br>

    {% for post in news %}
        <div>
            <h2>{{ post.title }}</h2>
            <h3>{{ post.short_description }}</h3>
            <p>{{ post.text }}</p>
            <small>Автор: {{ post.author.first_name }} {{ post.author.last_name }}</small>
            <small>Дата публикации: {{ post.pub_date }}</small>
        <hr>
        </div>
    {% endfor %}

{% endblock %}
